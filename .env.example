# Copy this file to `.env` and update values for your local setup.
# Keep real server-side secrets on your backend, not in Flutter.

API_BASE_URL=https://api.example.com
API_SECRET_KEY="mock_api_secret_key_dev_only"